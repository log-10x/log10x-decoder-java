ext {
	PUBLISH_ARTIFACT_DESC = "Core Log10x Java decoder library"
}

dependencies {
	implementation "org.unbescape:unbescape:${unbescapeVersion}"
}

apply from: "${rootProject.projectDir}/scripts/publish-module.gradle"
